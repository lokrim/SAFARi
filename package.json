{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"node ./src/server.js\" \"live-server --port=5500 --open=index.html\"",
    "server": "node ./src/server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.7",
    "concurrently": "^9.1.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "live-server": "^1.2.0",
    "nodemon": "^3.1.0"
  }
}
